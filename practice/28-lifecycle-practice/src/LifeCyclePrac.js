import React, { useEffect, useState } from "react";

export default function LifeCyclePrac() {
    const [post, setPosts] = useState([]);

    useEffect(() => {
        setTimeout;
    });

    return <div>LifeCyclePrac</div>;
}
